//
//  L10n.swift
//  FlexX
//
//  Created by Dhaarani M on 12/08/26.
//

import Foundation

enum L10n {

    static let loginTitle = NSLocalizedString(
        "login.title",
        comment: "Application name"
    )

    static let loginWelcome = NSLocalizedString(
        "login.welcome",
        comment: "Login welcome message"
    )

    static let loginSubtitle = NSLocalizedString(
        "login.subtitle",
        comment: "Login subtitle"
    )

    static let email = NSLocalizedString(
        "login.email",
        comment: "Email label"
    )

    static let emailPlaceholder = NSLocalizedString(
        "login.email.placeholder",
        comment: "Email placeholder"
    )

    static let password = NSLocalizedString(
        "login.password",
        comment: "Password label"
    )

    static let passwordPlaceholder = NSLocalizedString(
        "login.password.placeholder",
        comment: "Password placeholder"
    )

    static let loginButton = NSLocalizedString(
        "login.button",
        comment: "Login button"
    )

    static let orContinueWith = NSLocalizedString(
        "login.or.continueWith",
        comment: "Authentication separator"
    )

    static let googleLogin = NSLocalizedString(
        "login.google",
        comment: "Google login button"
    )

    static let signupPrompt = NSLocalizedString(
        "login.signup",
        comment: "Signup prompt"
    )
    
    static let ok = NSLocalizedString(
        "ok",
        comment: "OK"
    )
    
    static let signupTitle = NSLocalizedString(
        "signup.title",
        comment: "Signup title"
    )
    
    static let signupSubtitle = NSLocalizedString(
        "signup.subtitle",
        comment: "Signup subtitle"
    )
    
    static let signup = NSLocalizedString(
        "signup",
        comment: "Signup"
    )
    
    static let signupNametextfield = NSLocalizedString(
        "signup.nametextfield",
        comment: "Signup name"
    )
    
    static let signupEmailtextfield = NSLocalizedString(
        "signup.emailtextfield",
        comment: "Signup email"
    )
    
    static let signupPasswordtextfield = NSLocalizedString(
        "signup.passwordtextfield",
        comment: "Signup password"
    )
    
    static let signupConfirmpasswordtextfield = NSLocalizedString(
        "signup.confirmPasswordtextfield",
        comment: "Signup confirm password"
    )
    
    static let signupDobfield = NSLocalizedString(
        "signup.dobfield",
        comment: "Signup date of birth"
    )
    
    static let signupSignup = NSLocalizedString(
        "signup.signup",
        comment: "Signup button"
    )
    
    static let signupLogin = NSLocalizedString(
        "signup.login",
        comment: "Signup login"
    )
    
    static let signupAlerttologin = NSLocalizedString(
        "signup.alerttologin",
        comment: "Signup alert to login"
    )
    
    static let signupAlertmessage = NSLocalizedString(
        "signup.alertmessage",
        comment: "Signup alert message"
    )
    
    static let signupAlerttitle = NSLocalizedString(
        "signup.alerttitle",
        comment: "Signup alert title"
    )
    
    static let signupNamelabel = NSLocalizedString(
        "signup.namelabel",
        comment: "Signup name label"
    )
    
    static let signupEmaillabel = NSLocalizedString(
        "signup.emaillabel",
        comment: "Signup email label"
    )
    
    static let signupPasswordlabel = NSLocalizedString(
        "signup.passwordlabel",
        comment: "Signup password label"
    )
    
    static let signupConfirmPasswordlabel = NSLocalizedString(
        "signup.confirmPasswordlabel",
        comment: "Signup confirm password label"
    )
    
    static let signupDoblabel = NSLocalizedString(
        "signup.doblabel",
        comment: "Signup date of birth label"
    )
    
    static let tabHome = NSLocalizedString(
        "tab.home",
        comment: "Tab home"
    )
    
    static let tabProfile = NSLocalizedString(
        "tab.profile",
        comment: "Tab profile"
    )
    
    static let tabFavorite = NSLocalizedString(
        "tab.favorite",
        comment: "Tab favorites"
    )
    
    static let profileLogout = NSLocalizedString(
        "profile.logout",
        comment: "Profile logout"
    )
    
    static let editProfile = NSLocalizedString(
        "profile.editProfile",
        comment: "Profile edit profile"
    )
    
}
